# derived Postgres index data — droppable by design
.pg-data/
# persisted socket dir for deep repo paths (machine-local, stable across reboots)
.pg-socket-path

# nix / direnv
.direnv/
result

# built binary
tools/silo-kb/silo-kb

# local-only claude settings
.claude/settings.local.json
