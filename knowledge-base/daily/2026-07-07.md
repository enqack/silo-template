---
id: 0f228fb5-2fef-40e0-90ba-a74fe1bb3381
type: daily-log
title: 2026-07-07
timestamp: 2026-07-07 09:00:00
---

# 2026-07-07

## 09:00:00

### Concepts

- Postgres as a derived index over the OKF markdown tree: droppable, rebuildable from
  git HEAD in one command, never authoritative. See [[postgres-derived-index]].
- Two-tier vault: `knowledge/` holds decaying working theories, `projects/` holds
  asserted canon that never decays.
- The Jack Handey "Deep Thought" craft: a comedic bait-and-switch between a gentle,
  poetic setup and an unhinged deadpan payoff; a three-part structure and a specific
  narrator persona. See [[deep-thoughts-craft]].

### Cursed Knowledge

- Filesystem mtime is meaningless after a git checkout — any age-based rule has to use
  the git last-commit date instead.

### Unresolved

- [ ] Pick the reinforcement threshold that makes "sustained confidence" concrete.
- [ ] Decide how two NixOS boxes safely share one vault (per-resource mutation queue).

### Log

- Drafted the knowledge-base architecture: two-tier vault with graduation, confidence
  decay, hybrid RRF retrieval over pgvector + tsvector, Go MCP server.

### Environment

- The flake's `pg_ctl -o` quoting needed an unquoted empty `listen_addresses=` — nix
  indented-string escaping of `''` collides with shell quoting inside `-o` strings.
- DSN self-location (walk up to `.pg-data`) works from outside the dev shell, which is
  what the MCP server and hooks rely on.
